# Windowlogging Fabric

Windowlogging is a Create feature from 1.14.4 in which you can put windows (glass panes, iron bars) inside of other partial blocks like slabs and stairs. This mod is a heavily modified port of another port of that feature.

The lite version contains less aggressive mixin instrumentation which may result in less crashes or bugs in return for less QoL features.